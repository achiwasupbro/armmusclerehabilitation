# 🚨 ต้อง Deploy Server ก่อน!

## ปัญหา:
- ❌ ESP32 เชื่อมต่อไม่ได้เพราะ Server ยังไม่ทำงาน
- ❌ เว็บแสดง "เกิดข้อผิดพลาด Server"

## วิธีแก้:

### 1. Push โค้ดไป GitHub

```bash
git add .
git commit -m "WebSocket Server"
git push
```

### 2. Deploy บน Render

1. ไปที่ https://render.com
2. เข้าสู่ระบบด้วย GitHub
3. คลิก **"New +"** → **"Web Service"**
4. เลือก Repository ของคุณ
5. ตั้งค่า:
   - **Name**: `armmusclerehabilitation`
   - **Environment**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: Free
6. คลิก **"Create Web Service"**
7. รอ Deploy เสร็จ (~2-3 นาที)

### 3. ตรวจสอบว่า Server ทำงาน

เปิดเว็บบราวเซอร์:
```
https://armmusclerehabilitation.onrender.com/
```

ถ้าเห็นหน้าเว็บ = Deploy สำเร็จ ✅

### 4. ลองเชื่อมต่อ ESP32 อีกครั้ง

- Reset ESP32 (กดปุ่ม Reset)
- ดู Serial Monitor จะเห็น:
  ```
  ✅ WebSocket Connected
  ✅ ลงทะเบียนสำเร็จ
  ```

---

## 🔍 ตรวจสอบ Serial Monitor

จาก Serial Monitor ของคุณเห็น:
```
❌ WebSocket Disconnected (หลายครั้ง)
```

นี่แสดงว่า ESP32 พยายามเชื่อมต่อแต่ Server ยังไม่ทำงาน

---

## ⚡ ทางลัด: ทดสอบ Local ก่อน

ถ้าต้องการทดสอบก่อน Deploy:

### 1. รัน Server บนเครื่อง:
```bash
npm install
npm start
```

### 2. แก้ไข ESP32 ให้ใช้ Local:
```cpp
// แก้ไขใน wee_websocket_ssl.ino
const char* websocket_server = "192.168.1.XXX"; // IP ของคอมคุณ
const uint16_t websocket_port = 3000;
const bool use_ssl = false; // เปลี่ยนเป็น false
```

### 3. แก้ไข app.js:
ไม่ต้องแก้ - มันจะตรวจจับ localhost อัตโนมัติ

### 4. เปิดเว็บ:
```
http://localhost:3000/
```

---

## 📝 สรุป

**ปัญหาหลัก:** Server ยังไม่ได้ Deploy บน Render

**วิธีแก้:**
1. Deploy Server ไปยัง Render
2. รอให้ Deploy เสร็จ
3. Reset ESP32
4. เปิดเว็บอีกครั้ง

**หรือ:**
- ทดสอบ Local ก่อน (รัน `npm start` บนเครื่อง)
